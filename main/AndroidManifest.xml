<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shraddha.quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ComputerActivity" />
        <activity android:name=".EntertainmentActivity" />
        <activity android:name=".CapitalActivity" />
        <activity android:name=".GKActivity" />
        <activity android:name=".GTMActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HowmuchActivity" />
        <activity android:name=".CalculatorActivity" />
    </application>

</manifest>